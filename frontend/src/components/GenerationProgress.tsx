import type { ActivityGuide, LessonPlan } from "../types/teachingPack";

type Props = { planner: "pending" | "working" | "done"; designer: "pending" | "working" | "done"; plan: LessonPlan | null; guide: ActivityGuide | null };
const Step = ({ name, detail, status }: { name: string; detail: string; status: Props["planner"] }) => <div className={`rounded-3xl border p-5 transition ${status === "working" ? "border-[#195b4e] bg-[#edf6f1] shadow-sm" : status === "done" ? "border-emerald-200 bg-white" : "border-stone-200 bg-white/60"}`}><div className="flex items-center gap-3"><span className={`grid h-8 w-8 place-items-center rounded-full text-sm font-bold ${status === "done" ? "bg-emerald-700 text-white" : status === "working" ? "bg-[#195b4e] text-white animate-pulse" : "bg-stone-100 text-stone-400"}`}>{status === "done" ? "✓" : status === "working" ? "· · ·" : ""}</span><div><p className="font-semibold text-stone-900">{name}</p><p className="text-sm text-stone-500">{detail}</p></div></div></div>;
export function GenerationProgress({ planner, designer, plan, guide }: Props) { return <section className="mx-auto mt-12 max-w-3xl"><p className="text-center text-sm font-semibold tracking-wide text-[#195b4e]">CLARA ESTÁ PREPARANDO TU CLASE</p><h2 className="mt-3 text-center font-serif text-3xl text-stone-900">Dos miradas, una clase coherente.</h2><div className="mt-8 space-y-3"><Step name="Planificador" detail={plan ? `Objetivos y estructura listos: ${plan.title}` : "Mapeando objetivos y el arco de la clase"} status={planner}/><Step name="Diseñador" detail={guide ? `${guide.activities.length} actividades diseñadas para tu aula` : "Esperando el plan para crear experiencias de aula"} status={designer}/></div></section>; }
